"use strict";
// => Keyof
let chave;
chave = "novoKeyof";
// => Typeof
function coordenadasTypeof(xType, yType) {
    return { xType, yType };
}
let coordTypeof;
coordTypeof = (xType, yType) => {
    return {
        xType,
        yType,
    };
};
